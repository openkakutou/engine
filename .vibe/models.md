# Data models

No data models yet — only the module skeleton (`version.go`) exists. The match/combat state model (backlog item 001) will introduce the first real types (fighters, round/timer, position, facing).
